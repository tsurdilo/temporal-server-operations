# Temporal SDK — Java SDK (Micrometer reporter) Essential Alert Set
#
# Grafana alerting provisioning file. Drop into provisioning/alerting/ in your
# Grafana installation.
#
# Java Micrometer metric naming:
#   - Counters:   _total suffix (e.g. temporal_request_failure_total)
#   - Histograms: _seconds_bucket suffix
#   - Gauges:     no suffix (e.g. temporal_num_pollers, temporal_worker_task_slots_available)
#   - status_code label values: UPPER_CASE (e.g. NOT_FOUND, RESOURCE_EXHAUSTED)
#
# See temporal-sdk-java-micrometer.json dashboard (UID: temporal-sdk-java-micrometer-v1)
# See alerts-index.md for full descriptions and runbooks/ for triage guidance.

apiVersion: 1

groups:
  - orgId: 1
    name: temporal-sdk-java-micrometer-essential
    folder: Temporal SDK
    interval: 1m
    rules:

      - uid: temporal-sdk-java-micrometer-001a
        title: "Temporal SDK Java (Micrometer): NOT_FOUND on WFT Respond Operations"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="NOT_FOUND", operation=~"RespondWorkflowTaskCompleted|RespondWorkflowTaskFailed"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): NOT_FOUND on WFT respond — server timed out task or execution no longer running"
          description: |
            **1. Check WFT execution latency.**
            Look at `temporal_workflow_task_execution_latency` on the SDK dashboard for the affected namespace and task queue. If p99 is elevated and approaching or exceeding the WFT timeout (default 10s), the worker is consistently finishing too late and the server is timing out tasks.

            **2. Check WFT replay latency.**
            If WFT execution latency is elevated, check `temporal_workflow_task_replay_latency` next. High replay latency means the worker is spending too long re-executing history before reaching the new command — check for large history size and cross-check alert [#35 — WFT Replay Latency High](../alerts-index.md#alert-35--wft-replay-latency-high). Also check alert [#16 — Sticky Cache Forced Evictions High](../alerts-index.md#alert-16--sticky-cache-forced-evictions-high) and alert [#34 — Sticky Cache Disabled](../alerts-index.md#alert-34--sticky-cache-disabled) — high eviction rates force frequent cold replays which inflate execution latency.

            **3. Check worker pod resources.**
            Check CPU and memory utilization on the worker pods — high CPU slows WFT execution directly. Also check for cold-start issues on the pod that picked up the task: look at the `identity` field in the `WorkflowTaskStarted` history event to identify which worker pod ran the task.

            **4. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](../alerts-index.md#alert-2b--request-failure-resource_exhausted-on-respond-operations). On some SDK versions, sustained server throttling on `RespondWorkflowTaskCompleted` can cascade into a workflow task timeout — the task times out while the SDK is still retrying the throttled respond call.

            **5. Check server-side service and persistence latency.**
            If SDK-side metrics look normal and the execution was not terminated or timed out, check the server dashboard:
            - [**Frontend Service Latency** panel (§4 — Service Latencies)](../../../dashboards/server/temporal-server-readme.md) filtered to `RespondWorkflowTaskCompleted`
            - [**Persistence Latencies** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `UpdateWorkflowExecution`

            **6. Check whether the execution was explicitly terminated.**
            If the NOT_FOUND is on `RespondWorkflowTaskFailed` (not Completed), also check whether the execution was terminated intentionally — look at the `WorkflowExecutionTerminated` event in history and cross-check the **Workflow Terminate** panel on the server dashboard.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/01a-not-found-wft-respond.md

      - uid: temporal-sdk-java-micrometer-001b
        title: "Temporal SDK Java (Micrometer): NOT_FOUND on Activity Respond Operations"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="NOT_FOUND", operation=~"RespondActivityTaskCompleted|RespondActivityTaskFailed"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): NOT_FOUND on activity respond — task timed out or execution no longer running"
          description: |
            **1. Check activity execution latency.**
            Look at `temporal_activity_execution_latency` on the SDK dashboard for the affected `activity_type` and task queue. If p99 is elevated and approaching or exceeding the `startToClose` timeout, activities are consistently finishing too late — that is the direct cause.

            **2. Check worker pod resources.**
            Check CPU and memory utilization on the activity worker pods — high CPU slows activity execution directly. Also check for cold-start issues on the pod that picked up the task: look at the `identity` field in the `ActivityTaskStarted` history event, or in the pending activity info if this is not the final attempt.

            **3. Check whether the workflow execution is still running.**
            Look at the execution status in the Temporal UI or via `tctl`. If the execution completed, was terminated, or hit a run timeout, the NOT_FOUND is expected and not a signal of a timeout problem — check whether the execution lifecycle is behaving as expected.

            **4. Check for worker restarts.**
            If worker pods are restarting frequently during activity execution, in-flight task tokens are lost on restart. The server reschedules the activity but the original worker may still attempt to respond after coming back up. Check pod restart counts in your infrastructure observability stack.

            **5. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](../alerts-index.md#alert-2b--request-failure-resource_exhausted-on-respond-operations). Sustained throttling on `RespondActivityTaskCompleted` can delay the respond call long enough for the server to time out the task before the response lands.

            **6. Check server-side service and persistence latency.**
            If activity latency looks normal, check the server dashboard:
            - [**Frontend Service Latency** panel (§4 — Service Latencies)](../../../dashboards/server/temporal-server-readme.md) filtered to `RespondActivityTaskCompleted` and `RespondActivityTaskFailed`
            - [**Persistence Latencies** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `UpdateWorkflowExecution`
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/01b-not-found-activity-respond.md

      - uid: temporal-sdk-java-micrometer-001c
        title: "Temporal SDK Java (Micrometer): NOT_FOUND on RecordActivityTaskHeartbeat"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace) (
                  rate(temporal_request_failure_total{status_code="NOT_FOUND", operation="RecordActivityTaskHeartbeat"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: warning
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): NOT_FOUND on heartbeat — heartbeat timeout or activity timed out"
          description: |
            **1. Check the heartbeat interval vs heartbeatTimeout.**
            The worker must call heartbeat more frequently than the configured `heartbeatTimeout`. If the activity is slow between heartbeat calls — due to CPU pressure, blocking I/O, or downstream throttling — the effective heartbeat interval increases. Compare the activity's heartbeat call frequency in worker logs against its `heartbeatTimeout` setting.

            **2. Check worker CPU utilization.**
            A CPU-starved worker slows down between heartbeat calls even when the activity is making progress. Check per-pod CPU utilization — if consistently high, consider reducing `maxConcurrentActivityExecutionSize` to lower per-pod concurrency, or scale out horizontally.

            **3. Check for RESOURCE_EXHAUSTED on RecordActivityTaskHeartbeat.**
            Cross-check alert [#2d — RESOURCE_EXHAUSTED on Other Operations](../alerts-index.md#alert-2d--request-failure-resource_exhausted-on-other-operations). If the server is throttling heartbeat calls, the effective heartbeat interval increases and can push past `heartbeatTimeout` even when the worker is calling on time. Check the [**Resource Exhausted with Cause** panel (§6 — Throttling and Limits)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard.

            **4. Check startToClose timeout.**
            If the activity has been running longer than its `startToClose` timeout, the server times it out at the history level while the activity is still executing — the next heartbeat then returns NOT_FOUND. Check `temporal_activity_execution_latency` for the affected `activity_type` against the configured `startToClose` timeout.

            **5. Check heartbeat payload size.**
            The last heartbeat details payload is stored in shard mutable state and held in history service memory for the life of the activity attempt. Large heartbeat payloads on high-throughput activity workers can contribute to history host memory pressure. Keep heartbeat payloads small — store only the minimum progress state needed to resume on retry.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/01c-not-found-heartbeat.md

      - uid: temporal-sdk-java-micrometer-002a
        title: "Temporal SDK Java (Micrometer): RESOURCE_EXHAUSTED on Critical User-Facing Operations"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="RESOURCE_EXHAUSTED", operation=~"StartWorkflowExecution|SignalWithStartWorkflowExecution|ExecuteMultiOperation|UpdateWorkflowExecution|SignalWorkflowExecution"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 1m
        labels:
          severity: critical
          service: temporal-sdk
          component: request
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): server throttling critical user-facing operations (starts, signals, updates)"
          description: |
            **1. Check the server Resource Exhausted panel.**
            Check the [**Resource Exhausted with Cause** panel (§6 — Throttling and Limits)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — this shows the throttle cause (`RpsLimit`, `ConcurrentLimit`, `SystemOverloaded`, `CircuitBreakerOpen`) and helps identify whether this is a namespace RPS limit, a system-wide overload, or a circuit breaker firing.

            **2. Check persistence latency.**
            Check the [**Persistence Latencies** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `CreateWorkflowExecution` and `UpdateWorkflowExecution` — high persistence latency is the most common root cause of cascading throttling on these operations. If the database is slow, the frontend service backs up and starts throttling callers.

            **3. Check namespace RPS limits.**
            If the cause is `RpsLimit`, check whether `frontend.namespaceRPS` is set too low for your current traffic volume. Consider increasing it via dynamic config — but only after confirming persistence latency is healthy, otherwise increasing RPS into an already overloaded system will make things worse.

            **4. Check total cluster RPS.**
            Check the [**Total RPS** panel (§1 — Cluster Throughput)](../../../dashboards/server/temporal-server-readme.md) — if total cluster RPS is at or near capacity, the problem is cluster-level saturation, not namespace limits. Consider scaling frontend pods or the underlying database.

            **5. Check for system overload.**
            If the cause is `SystemOverloaded` or `CircuitBreakerOpen`, cross-check server alert [#30 — System Overload Throttling](../../server/runbooks/30-system-overload-throttling.md) — this indicates the server itself is under severe pressure and is shedding load to protect itself.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/02a-resource-exhausted-critical-ops.md

      - uid: temporal-sdk-java-micrometer-002b
        title: "Temporal SDK Java (Micrometer): RESOURCE_EXHAUSTED on Respond Operations"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="RESOURCE_EXHAUSTED", operation=~"RespondWorkflowTaskCompleted|RespondWorkflowTaskFailed|RespondActivityTaskCompleted|RespondActivityTaskFailed"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: request
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): server throttling respond operations — leading indicator for task timeouts"
          description: |
            **1. Check the server Resource Exhausted panel.**
            Check the [**Resource Exhausted with Cause** panel (§6 — Throttling and Limits)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — identify the throttle cause (`RpsLimit`, `ConcurrentLimit`, `SystemOverloaded`, `CircuitBreakerOpen`).

            **2. Check persistence latency.**
            Check the [**Persistence Latencies** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `UpdateWorkflowExecution` — high persistence latency on this operation is the most common root cause of throttling on respond operations. The frontend service backs up when the database is slow and starts shedding load.

            **3. Check namespace RPS limits.**
            If the cause is `RpsLimit`, consider increasing `frontend.namespaceRPS` via dynamic config — but only after confirming persistence latency is healthy.

            **4. Check for cascading timeouts.**
            Cross-check alert [#1a — NOT_FOUND on WFT Respond Operations](./01a-not-found-wft-respond.md) and alert [#1b — NOT_FOUND on Activity Respond Operations](./01b-not-found-activity-respond.md) — if those are also firing, throttling has already cascaded into task timeouts.

            **5. Check for system overload.**
            If the cause is `SystemOverloaded` or `CircuitBreakerOpen`, cross-check server alert [#30 — System Overload Throttling](../../server/runbooks/30-system-overload-throttling.md).
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/02b-resource-exhausted-respond-ops.md

      - uid: temporal-sdk-java-micrometer-005
        title: "Temporal SDK Java (Micrometer): Request Failure UNIMPLEMENTED"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="UNIMPLEMENTED"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 2m
        labels:
          severity: critical
          service: temporal-sdk
          component: request
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): UNIMPLEMENTED from server — wrong binary or version mismatch"
          description: |
            **1. Check your server deployment.**
            This is the most common cause. Check whether the correct server binary is running — a wrong binary deployed to one or more pods, a corrupted deployment, or a service routing issue sending requests to the wrong handler can all produce UNIMPLEMENTED responses on valid operations. Check pod versions across all history, matching, and frontend pods.

            **2. Check the Service Panics panel.**
            Check the [**Service Panics** panel (§5 — Service Requests and Errors)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — a wrong binary or corrupted deployment often produces panics alongside UNIMPLEMENTED responses. Any non-zero value here is a critical signal.

            **3. Check the Service Errors panel.**
            Check the [**Service Errors by Namespace** panel (§5)](../../../dashboards/server/temporal-server-readme.md) — a spike in errors correlated with the UNIMPLEMENTED alert confirms the server is not processing requests normally.

            **4. Check SDK version compatibility.**
            If the deployment looks correct, check whether you are running a very old SDK version that is calling an API that has been removed or significantly changed on the server. Review the Temporal SDK changelog for breaking API changes between your SDK version and your server version.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/05-unimplemented.md

      - uid: temporal-sdk-java-micrometer-006
        title: "Temporal SDK Java (Micrometer): Request Failure INTERNAL"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, operation) (
                  rate(temporal_request_failure_total{status_code="INTERNAL"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 2m
        labels:
          severity: critical
          service: temporal-sdk
          component: request
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "5"
          summary: "Temporal SDK Java (Micrometer): INTERNAL errors from server — infrastructure issue"
          description: |
            **1. Check the Service Panics panel.**
            Check the [**Service Panics** panel (§5 — Service Requests and Errors)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — any panic is a critical signal and is almost always the root cause of sustained INTERNAL errors. Investigate the panic message immediately.

            **2. Check the Service Errors panel.**
            Check the [**Service Errors by Namespace** panel (§5)](../../../dashboards/server/temporal-server-readme.md) — confirm the scope of errors. If errors are isolated to a single namespace, the problem may be namespace-specific. If errors are cluster-wide, the problem is infrastructure-level.

            **3. Check persistence errors and availability.**
            Check the [**Persistence Errors by Namespace and Operation** panel (§3)](../../../dashboards/server/temporal-server-readme.md) and the [**Persistence Availability** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) — sustained persistence errors are a common root cause of INTERNAL responses. If the database is returning errors, the server wraps them as INTERNAL and returns them to the caller.

            **4. Check server health and deployment.**
            Verify all server pods are healthy and running the correct binary. Check pod logs for stack traces or error messages. If a recent deployment was made, consider rolling back.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/06-internal.md

      - uid: temporal-sdk-java-micrometer-012
        title: "Temporal SDK Java (Micrometer): RespondWorkflowTaskCompleted Rate Dropped to Zero"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                (
                  sum by (namespace, task_queue) (
                    rate(temporal_request_total{operation="RespondWorkflowTaskCompleted"}[5m])
                  ) or vector(0)
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1]
                    type: lt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "3"
          summary: "Temporal SDK Java (Micrometer): WFT completions dropped to zero — workflow progress halted"
          description: |
            **1. Check if polling has also stopped.**
            Check `temporal_num_pollers` for `worker_type=WorkflowWorker` on the SDK dashboard. If pollers are also at zero, workers are down entirely — cross-check alert [#15 — All Pollers Disconnected](./15-all-pollers-disconnected.md) and check auth failure alert [#4](../alerts-index.md#alert-4--request-failure-authentication--authorization-failure) and UNIMPLEMENTED alert [#5](./05-unimplemented.md). Check worker pod health in your infrastructure observability stack.

            **2. Check if workers are failing tasks instead of completing them.**
            If polling is active but completions are zero, check `temporal_workflow_task_execution_failed` on the SDK dashboard — workers may be consistently failing tasks. Cross-check alert [#21 — WFT Execution Failed: NonDeterminismError](./21-nde.md) and alert [#22 — WFT Execution Failed: GrpcMessageTooLarge](./22-grpc-message-too-large.md).

            **3. Check worker task slots.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=WorkflowWorker` — if all slots are occupied, the SDK blocks before issuing the next poll and no new tasks are picked up or completed.

            **4. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](./02b-resource-exhausted-respond-ops.md) — sustained throttling on `RespondWorkflowTaskCompleted` suppresses this counter directly since the SDK only increments it after a successful response.

            **5. Check WFT schedule-to-start latency.**
            Cross-check alerts [#19](./19-wft-schedule-to-start-critical.md) and [#19b](./19b-wft-schedule-to-start-severe.md) — if tasks are not being dispatched to workers, completions will drop to zero even when workers are healthy.

            **6. Check overall cluster health.**
            Check the server dashboard: [**Service Errors by Namespace** (§5)](../../../dashboards/server/temporal-server-readme.md), [**Persistence Availability** (§3)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors), and [**Resource Exhausted with Cause** (§6)](../../../dashboards/server/temporal-server-readme.md).
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/12-wft-completions-zero.md

      - uid: temporal-sdk-java-micrometer-013
        title: "Temporal SDK Java (Micrometer): RespondActivityTaskCompleted Rate Dropped to Zero"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                (
                  sum by (namespace, task_queue) (
                    rate(temporal_request_total{operation="RespondActivityTaskCompleted"}[5m])
                  ) or vector(0)
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1]
                    type: lt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "3"
          summary: "Temporal SDK Java (Micrometer): activity completions dropped to zero — workflows stalled waiting on results"
          description: |
            **1. Check if polling has also stopped.**
            Check `temporal_num_pollers` for `worker_type=ActivityWorker` on the SDK dashboard. If pollers are also at zero, workers are down entirely — cross-check alert [#15 — All Pollers Disconnected](./15-all-pollers-disconnected.md) and check auth failure alert [#4](../alerts-index.md#alert-4--request-failure-authentication--authorization-failure) and UNIMPLEMENTED alert [#5](./05-unimplemented.md). Check worker pod health in your infrastructure observability stack.

            **2. Check for RESOURCE_EXHAUSTED on poll operations.**
            Cross-check alert [#2c](../alerts-index.md#alert-2c--request-failure-resource_exhausted-on-long-poll-operations) — if the server is throttling `PollActivityTaskQueue`, workers back off and pick up tasks less frequently, which can suppress completions even when workers are alive.

            **3. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](./02b-resource-exhausted-respond-ops.md) — sustained throttling on `RespondActivityTaskCompleted` suppresses this counter directly since the SDK only increments it after a successful response.

            **4. Check worker task slots.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=ActivityWorker` — if all slots are occupied, the SDK blocks before issuing the next poll and no new tasks are picked up or completed.

            **5. Check for workflow task-level problems.**
            Activity completions drop to zero when no new activities are being scheduled — which happens when workflow tasks are not completing. Cross-check alert [#12 — RespondWorkflowTaskCompleted Rate Dropped to Zero](./12-wft-completions-zero.md), alert [#21 — WFT Execution Failed: NonDeterminismError](./21-nde.md), alert [#22 — WFT Execution Failed: GrpcMessageTooLarge](./22-grpc-message-too-large.md), and WFT schedule-to-start alerts [#19](./19-wft-schedule-to-start-critical.md) and [#19b](./19b-wft-schedule-to-start-severe.md).

            **6. Check overall cluster health.**
            Check the server dashboard: [**Service Errors by Namespace** (§5)](../../../dashboards/server/temporal-server-readme.md), [**Persistence Availability** (§3)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors), and [**Resource Exhausted with Cause** (§6)](../../../dashboards/server/temporal-server-readme.md).
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/13-activity-completions-zero.md

      - uid: temporal-sdk-java-micrometer-014
        title: "Temporal SDK Java (Micrometer): Worker Task Slots Exhausted"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, worker_type, task_queue) (
                  temporal_worker_task_slots_available
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1]
                    type: lt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 2m
        labels:
          severity: critical
          service: temporal-sdk
          component: worker
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "34"
          summary: "Temporal SDK Java (Micrometer): all {{ $labels.worker_type }} slots exhausted on {{ $labels.task_queue }}"
          description: |
            ## WorkflowWorker

            All workflow task execution slots are occupied. Existing tasks are taking too long to complete and not releasing their slots back to the pool.

            **Triage steps:**

            **1. Check WFT execution latency.**
            Check `temporal_workflow_task_execution_latency` on the SDK dashboard — sustained high values confirm something is holding slots. Cross-check alert [#32 — WFT Execution Latency Critical](./32-wft-execution-latency-critical.md).

            **2. Check worker pod CPU.**
            High CPU slows WFT execution directly and keeps slots occupied longer. Check per-pod CPU utilization in your infrastructure observability stack.

            **3. Check for blocking calls in workflow code.**
            Slots are held until the WFT completes. If workflow code is performing blocking I/O, heavy computation, or calling non-Temporal APIs synchronously, the WFT will hold its slot far longer than expected. For Python SDK workers, verify that no `async def` workflow code is blocking the event loop.

            **4. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](./02b-resource-exhausted-respond-ops.md) — if the server is throttling `RespondWorkflowTaskCompleted`, slots are not released until the respond call succeeds.

            **Immediate action:** Scale up workflow worker pods or increase `maxConcurrentWorkflowTaskExecutionSize`.

            ---

            ## ActivityWorker

            All activity execution slots are occupied. Existing activities are taking too long to complete and not releasing their slots.

            **Triage steps:**

            **1. Check activity execution latency.**
            Check `temporal_activity_execution_latency` for the affected task queue and `activity_type` — sustained high values confirm activities are holding slots far longer than expected.

            **2. Check worker pod CPU.**
            High CPU slows activity execution directly. Check per-pod CPU utilization.

            **3. Check for downstream throttling.**
            If activity slots are exhausted because activities are blocked waiting on a downstream service, increasing slot counts will only increase pressure on that downstream service and make the problem worse. Investigate what the activities are waiting on before scaling up concurrency.

            **4. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](./02b-resource-exhausted-respond-ops.md) — if the server is throttling `RespondActivityTaskCompleted`, slots are not released until the respond call succeeds.

            **Immediate action:** Scale up activity worker pods or increase `maxConcurrentActivityExecutionSize`.

            ---

            ## LocalActivityWorker

            All local activity execution slots are occupied. The worker cannot schedule or pick up new local activities.

            **Triage steps:**

            **1. Understand the impact.**
            Local activities run within the WFT execution loop — blocked slots hold up the entire WFT. The SDK responds by sending repeated WFT heartbeats to keep the WFT alive. Cross-check alert [#25 — WFT Heartbeat Rate Elevated](../alerts-index.md#alert-25--wft-heartbeat-rate-elevated). If heartbeating continues past `history.workflowTaskHeartbeatTimeout` (default 30 minutes), the server will time out the heartbeat WFT and reschedule it — local activities will be re-executed from scratch with potential duplicate side effects if not idempotent.

            **2. Check what local activities are waiting on.**
            The root cause is almost always local activity code that is blocking and not returning. Check worker logs for the affected `activity_type`. If they are calling downstream services, check whether those services are throttling or slow — increasing slot counts in that scenario would only increase downstream pressure.

            **3. Check worker pod CPU.**
            High CPU slows local activity execution directly. Check per-pod CPU utilization.

            **4. Cross-check local activity latency alerts.**
            Cross-check alert [#29 — Local Activity Execution Latency Exceeds WFT Heartbeat Timeout](./29-la-execution-latency-timeout.md) and alert [#30 — Local Activity Total Execution Latency Exceeds WFT Heartbeat Timeout](./30-la-total-latency-timeout.md) — if either is firing alongside this alert, the WFT heartbeat timeout is imminent or already exceeded.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/14-worker-slots-exhausted.md

      - uid: temporal-sdk-java-micrometer-015
        title: "Temporal SDK Java (Micrometer): All Pollers Disconnected"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, worker_type, task_queue) (
                  temporal_num_pollers
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1]
                    type: lt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: Alerting
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: poller
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "36"
          summary: "Temporal SDK Java (Micrometer): no active {{ $labels.worker_type }} pollers on {{ $labels.task_queue }}"
          description: |
            **1. Check if worker pods are running.**
            Check pod status, restart counts, and logs in your infrastructure observability stack — workers may have crashed, been evicted, or OOM killed. This is the most common cause of pollers dropping to zero. If pods are down, bring them back up and verify they start cleanly.

            **2. Check if slots are exhausted.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for the same `worker_type` and task queue. Both Go and Java SDKs block on slot acquisition before incrementing `temporal_num_pollers` — so if all slots are occupied, the gauge drops to zero as a secondary effect. In that case #14 is the root cause and this alert is a symptom. Fix #14 first.

            **3. Check for auth failures.**
            Cross-check alert [#4](../alerts-index.md#alert-4--request-failure-authentication--authorization-failure) — expired or revoked credentials are a common cause of pollers disconnecting. On the server side, check the [**Unauthorized Requests** panel (§19 — Authorization)](../../../dashboards/server/temporal-server-readme.md) and the [**Authorization System Failures** panel (§19)](../../../dashboards/server/temporal-server-readme.md) — a non-zero value on Authorization System Failures indicates the auth plugin itself is failing, which is more urgent than a simple credential problem.

            **4. Check for INTERNAL errors.**
            Cross-check alert [#6 — INTERNAL](./06-internal.md) — sustained INTERNAL errors from the server can cause workers to back off and stop polling. Check the [**Service Panics** panel (§5)](../../../dashboards/server/temporal-server-readme.md) and [**Persistence Availability** panel (§3)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) on the server dashboard.

            **5. Check the server-side poller count.**
            Cross-check the [**Total Concurrent Pollers** panel (§15 — Pollers)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — if the server-side concurrent poller count for this task queue has also dropped, it confirms workers have fully disconnected from the server's perspective.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/15-all-pollers-disconnected.md

      - uid: temporal-sdk-java-micrometer-019
        title: "Temporal SDK Java (Micrometer): WFT Schedule-To-Start Latency Critical (p99 > 5s)"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, task_queue, le) (
                    rate(temporal_workflow_task_schedule_to_start_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [5]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "22"
          summary: "Temporal SDK Java (Micrometer): WFT schedule-to-start p99 > 5s on {{ $labels.task_queue }}"
          description: |
            **1. Check worker pod health.**
            Check that worker pods are running and healthy in your infrastructure observability stack. If pods are down or restarting, bring them back up first.

            **2. Check poller counts.**
            Check `temporal_num_pollers` for `worker_type=WorkflowWorker` on the SDK dashboard — if pollers are dropping, cross-check alert [#15 — All Pollers Disconnected](./15-all-pollers-disconnected.md). Fewer pollers means fewer workers competing for tasks, which directly increases schedule-to-start latency.

            **3. Check worker task slots.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=WorkflowWorker` — if all slots are occupied, the SDK blocks before issuing the next poll, reducing effective poll rate and increasing schedule-to-start latency.

            **4. Check for RESOURCE_EXHAUSTED on poll operations.**
            Cross-check alert [#2c](../alerts-index.md#alert-2c--request-failure-resource_exhausted-on-long-poll-operations) filtered to `PollWorkflowTaskQueue` — if the server is throttling poll operations, workers back off and poll less frequently.

            **5. Check the task backlog on the server.**
            Check the [**Approximate Task Backlog** panel](../../../dashboards/server/temporal-server-readme.md) in the Matching service section of the server dashboard — a growing backlog confirms tasks are accumulating faster than workers can pick them up.

            **6. Check the server-side concurrent poller count.**
            Check the [**Total Concurrent Pollers** panel (§15 — Pollers)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — if the concurrent poller count for workflow tasks has dropped, your worker pool may have shrunk and horizontal scaling is needed.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/19-wft-schedule-to-start-critical.md

      - uid: temporal-sdk-java-micrometer-019b
        title: "Temporal SDK Java (Micrometer): WFT Schedule-To-Start Latency Severely Elevated (p99 > 30m)"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, task_queue, le) (
                    rate(temporal_workflow_task_schedule_to_start_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1800]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "22"
          summary: "Temporal SDK Java (Micrometer): WFT schedule-to-start p99 > 30m on {{ $labels.task_queue }} — large backlog risk"
          description: |
            **1. Check worker pod health.**
            Check that worker pods are running and healthy in your infrastructure observability stack. If pods are down or restarting, bring them back up immediately — this is the most common cause of severe schedule-to-start latency.

            **2. Check poller counts.**
            Check `temporal_num_pollers` for `worker_type=WorkflowWorker` on the SDK dashboard. If pollers have dropped to zero, cross-check alert [#15 — All Pollers Disconnected](./15-all-pollers-disconnected.md) and follow that runbook first.

            **3. Check worker task slots.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=WorkflowWorker` — if all slots are occupied, no new tasks can be picked up regardless of how many pollers are active.

            **4. Check the task backlog on the server.**
            Check the [**Approximate Task Backlog** panel](../../../dashboards/server/temporal-server-readme.md) in the Matching service section of the server dashboard — at this latency level the backlog is likely already very large. A large backlog means even after workers recover, it will take time to drain before schedule-to-start latency returns to normal.

            **5. Check the server-side concurrent poller count.**
            Check the [**Total Concurrent Pollers** panel (§15 — Pollers)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — if the concurrent poller count for workflow tasks has dropped significantly, scale out worker pods horizontally to recover polling capacity faster.

            **6. Check for RESOURCE_EXHAUSTED on poll operations.**
            Cross-check alert [#2c](../alerts-index.md#alert-2c--request-failure-resource_exhausted-on-long-poll-operations) filtered to `PollWorkflowTaskQueue` — if the server is throttling poll operations at this scale, increasing the namespace concurrent poller limit via `frontend.namespaceCount` or `frontend.globalNamespaceCount` may be needed alongside scaling workers.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/19b-wft-schedule-to-start-severe.md

      - uid: temporal-sdk-java-micrometer-020b
        title: "Temporal SDK Java (Micrometer): Activity Schedule-To-Start Latency Severely Elevated (p99 > 30m)"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, activity_type, task_queue, le) (
                    rate(temporal_activity_schedule_to_start_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1800]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "23"
          summary: "Temporal SDK Java (Micrometer): activity schedule-to-start p99 > 30m on {{ $labels.task_queue }} — large backlog risk"
          description: |
            **1. Check worker pod health.**
            Check that activity worker pods are running and healthy in your infrastructure observability stack. If pods are down or restarting, bring them back up immediately — this is the most common cause of severe schedule-to-start latency.

            **2. Check poller counts.**
            Check `temporal_num_pollers` for `worker_type=ActivityWorker` on the SDK dashboard. If pollers have dropped to zero, cross-check alert [#15 — All Pollers Disconnected](./15-all-pollers-disconnected.md) and follow that runbook first.

            **3. Check worker task slots.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=ActivityWorker` — if all slots are occupied, no new tasks can be picked up regardless of how many pollers are active.

            **4. Check the task backlog on the server.**
            Check the [**Approximate Task Backlog** panel](../../../dashboards/server/temporal-server-readme.md) in the Matching service section of the server dashboard — at this latency level the backlog is likely already very large. A large backlog means even after workers recover, it will take time to drain before schedule-to-start latency returns to normal.

            **5. Check the server-side concurrent poller count.**
            Check the [**Total Concurrent Pollers** panel (§15 — Pollers)](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — if the concurrent poller count for activity tasks has dropped significantly, scale out activity worker pods horizontally to recover polling capacity faster.

            **6. Check for RESOURCE_EXHAUSTED on poll operations.**
            Cross-check alert [#2c](../alerts-index.md#alert-2c--request-failure-resource_exhausted-on-long-poll-operations) filtered to `PollActivityTaskQueue` — if the server is throttling poll operations at this scale, increasing the namespace concurrent poller limit via `frontend.namespaceCount` or `frontend.globalNamespaceCount` may be needed alongside scaling workers.

            **7. Check activity failure rates.**
            Cross-check alert [#26 — Activity Execution Failed Rate Elevated](./26-activity-execution-failed.md) — high churn of unexpected activity failures generates additional retry tasks, which can contribute to backlog growth and keep schedule-to-start latency elevated even after worker capacity is restored.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/20b-activity-schedule-to-start-severe.md

      - uid: temporal-sdk-java-micrometer-021
        title: "Temporal SDK Java (Micrometer): WFT Execution Failed — NonDeterminismError"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, workflow_type, task_queue, failure_reason) (
                  rate(temporal_workflow_task_execution_failed_total{failure_reason="NonDeterminismError"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 1m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "47"
          summary: "Temporal SDK Java (Micrometer): NonDeterminismError on {{ $labels.workflow_type }} — executions stuck"
          description: |
            **1. Identify the affected workflow IDs.**
            This metric does not carry `workflow_id` — check worker logs, which will log the NDE with the associated workflow ID and run ID. In the Temporal UI, affected executions can also be found by querying the `TemporalReportedProblems` search attribute, which the server sets on executions experiencing repeated workflow task failures.

            **2. Understand the error.**
            Look at the `WorkflowTaskFailed` event in the history of an affected execution — the event contains the error message that indicates exactly where in the replay the mismatch occurred and what command was expected vs. what the code produced. This is the most direct signal for root cause analysis.

            **3. Determine whether this is a code change or a deployment issue.**
            Common causes:
            - A code change added, removed, or reordered workflow commands (activity schedules, timers, signals, child workflows) without a versioning guard (`workflow.GetVersion` in Go, `Workflow.getVersion` in Java) — in-flight executions that already produced history under the old code will NDE on the new code
            - A rolling restart where old and new worker versions are briefly running simultaneously — some executions may NDE transiently while the old version is still partially deployed; these typically resolve once the rollout completes
            - Changing activity or timer parameters in existing workflow code without versioning

            **4. Roll back if needed.**
            If NDE started after a deployment and is not resolving on its own (i.e. not a transient rolling restart issue), roll back the worker to the previous version — affected executions will resume on their next WFT retry once the compatible code is running. Then fix the incompatible change by introducing a proper versioning guard before redeploying.

            **5. Monitor worker pressure.**
            While NDE is active, the server keeps retrying the workflow task — this puts sustained pressure on workflow workers. Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=WorkflowWorker` and alert [#32 — WFT Execution Latency Critical](./32-wft-execution-latency-critical.md) — a high volume of NDE retries can saturate worker capacity and affect healthy executions on the same task queue.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/21-nde.md

      - uid: temporal-sdk-java-micrometer-022
        title: "Temporal SDK Java (Micrometer): WFT Execution Failed — GrpcMessageTooLarge"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, workflow_type, task_queue, failure_reason) (
                  rate(temporal_workflow_task_execution_failed_total{failure_reason="GrpcMessageTooLarge"}[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 1m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "47"
          summary: "Temporal SDK Java (Micrometer): GrpcMessageTooLarge on {{ $labels.workflow_type }} — executions being terminated"
          description: |
            **1. Identify the affected workflow IDs.**
            This metric does not carry `workflow_id` — check worker logs for the affected workflow IDs and run IDs. Look at the `WorkflowTaskFailed` event and the `WorkflowExecutionTerminated` event in the history of affected executions to confirm the cause.

            **2. Investigate which part of the WFT response is oversized.**
            The fix depends entirely on what is making the response too large. Common causes:
            - **Oversized activity inputs or outputs** — activity arguments or return values that are too large. Move large payloads out of band (e.g. store in blob storage and pass a reference) instead of passing them directly through Temporal history.
            - **Excessive signal or update accumulation in history** — a large number of signals or updates buffered in a single WFT. Consider rate-limiting signal senders or batching signals.
            - **Too many commands batched in a single WFT response** — a workflow scheduling a very large number of activities or child workflows in a single execution step. Break large fan-outs into smaller batches across multiple WFTs.

            **3. Check for terminated executions on the server.**
            Check the [**Workflow Terminate** panel](../../../dashboards/server/temporal-server-readme.md) on the server dashboard — a spike in terminated executions alongside this alert confirms the server is terminating executions in response to this failure.

            **4. Fix the root cause and redeploy first.**
            Terminated executions do not retry automatically. Before restarting any executions, fix the underlying cause — oversized payloads, excessive signal accumulation, or large fan-outs — and deploy the corrected worker. Restarting executions without fixing the code will cause them to hit the same limit and be terminated again. Once the fix is deployed and verified, restart affected executions manually via the Temporal UI or CLI if they need to continue.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/22-grpc-message-too-large.md

      - uid: temporal-sdk-java-micrometer-026
        title: "Temporal SDK Java (Micrometer): Activity Execution Failed Rate Elevated"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, activity_type, task_queue) (
                  rate(temporal_activity_execution_failed_total[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [20]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: warning
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "63"
          summary: "Temporal SDK Java (Micrometer): activity failure rate > 20/s on {{ $labels.activity_type }}"
          description: |
            **1. Identify which activity type is failing.**
            The metric carries an `activity_type` label — use it to narrow down which activity is the source. Check worker logs for the affected `activity_type` to get the specific error messages and stack traces. Worker logs will also include the workflow ID associated with each failure.

            **2. Investigate the root cause.**
            Check whether this is a transient infrastructure issue (downstream service unavailable, network partition, database timeout) or a persistent code bug. Transient failures will naturally recover — monitor whether the rate drops on its own. Persistent failures require a code fix.

            **3. Check downstream service health.**
            If activities are calling external services, check the health of those services. A degraded downstream service is a common cause of sustained activity failure bursts. If the downstream is throttling, check whether your activity retry policy has appropriate backoff — without backoff, retry bursts can amplify downstream pressure.

            **4. Check activity schedule-to-start latency.**
            Cross-check alert [#20b — Activity Schedule-To-Start Latency Severely Elevated](./20b-activity-schedule-to-start-severe.md) — a growing retry backlog will show up there as elevated schedule-to-start latency even after the failure rate drops.

            **5. Consider using benign failures for expected failures.**
            If your use case intentionally fails activities by design — polling patterns, saga compensations, flow control via exceptions — mark those `ApplicationFailure` instances with category `BENIGN`. All SDKs support this and will suppress this metric for those failures, making this alert exclusively track unexpected failures without needing per-`activity_type` threshold tuning.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/26-activity-execution-failed.md

      - uid: temporal-sdk-java-micrometer-027
        title: "Temporal SDK Java (Micrometer): Unregistered Activity Invocation"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, activity_type, task_queue) (
                  rate(temporal_unregistered_activity_invocation_total[5m])
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [0]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 1m
        labels:
          severity: critical
          service: temporal-sdk
          component: activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "65"
          summary: "Temporal SDK Java (Micrometer): unregistered activity {{ $labels.activity_type }} — deployment or routing issue"
          description: |
            **1. Identify the unregistered activity type.**
            The metric carries an `activity_type` label — use it to identify exactly which activity type is missing. Check worker logs for additional context.

            **2. Check your worker registration.**
            Verify that the activity type shown in the alert is registered in the worker that is polling on the affected task queue. Check your worker startup code — the activity must be explicitly registered before the worker starts polling.

            **3. Check for a deployment issue.**
            If the activity was recently added or renamed, check whether the correct worker binary is deployed. A rolling deploy where old worker versions are still running will cause the old workers to receive tasks for newly registered activity types they do not know about. Wait for the rollout to complete or accelerate it.

            **4. Check for a routing issue.**
            Verify that the task queue name in the workflow code matches the task queue the worker is polling on. A mismatch — for example, a typo in the task queue name — will route activity tasks to workers that are not configured to handle them.

            **5. Check for a worker restart during activity execution.**
            If a worker was restarted mid-execution with a different binary that is missing the activity registration, in-flight tasks will be re-dispatched to a worker that cannot handle them. Check recent worker deployment history and pod restart counts.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/27-unregistered-activity.md

      - uid: temporal-sdk-java-micrometer-029
        title: "Temporal SDK Java (Micrometer): Local Activity Execution Latency Exceeds WFT Heartbeat Timeout"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, activity_type, task_queue, le) (
                    rate(temporal_local_activity_execution_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1800]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: local-activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "74"
          summary: "Temporal SDK Java (Micrometer): local activity {{ $labels.activity_type }} single attempt > 30m — WFT heartbeat timeout imminent"
          description: |
            **1. Identify the affected local activity type.**
            The metric carries an `activity_type` label. Check worker logs for the affected `activity_type` and associated workflow IDs — logs will show what the local activity is doing and how long individual attempts are running.

            **2. Investigate what the local activity is blocked on.**
            Local activities running this long are almost always blocked on a downstream call — a slow external service, a database query, or a network timeout with a very long timeout setting. Check worker logs for the affected `activity_type` and check the health of any downstream services it calls. Fix the downstream issue or reduce the timeout on the downstream call so the local activity fails fast rather than hanging.

            **3. Check whether WFT heartbeat timeouts have already occurred.**
            By the time this alert fires, the server may have already timed out heartbeat WFTs and rescheduled them — check worker logs for WFT timeout errors and check the history of affected executions for `WorkflowTaskTimedOut` events. If timeouts have occurred, local activities have already been re-executed from scratch — verify whether they are idempotent and whether any duplicate side effects need to be addressed.

            **4. Check whether LA slots are exhausted.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=LocalActivityWorker` — if multiple local activities are running long simultaneously, all available slots can become occupied and no new local activities can start.

            **5. Fix the design.**
            If the local activity must perform long-running work, convert it to a regular activity with heartbeating — this is the correct primitive for work that runs longer than a few seconds. If it must remain a local activity, always set a `scheduleToCloseTimeout` less than 30 minutes so it fails with a timeout error before the server times out the WFT heartbeat, allowing the workflow to handle the failure gracefully rather than having the entire WFT re-executed from scratch.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/29-la-execution-latency-timeout.md

      - uid: temporal-sdk-java-micrometer-030
        title: "Temporal SDK Java (Micrometer): Local Activity Total Execution Latency Exceeds WFT Heartbeat Timeout"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, activity_type, task_queue, le) (
                    rate(temporal_local_activity_total_execution_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1800]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: local-activity
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "76"
          summary: "Temporal SDK Java (Micrometer): local activity {{ $labels.activity_type }} retry chain > 30m — WFT heartbeat timeout imminent"
          description: |
            **1. Identify the affected local activity type.**
            The metric carries an `activity_type` label. Check worker logs for the affected `activity_type` and associated workflow IDs — logs will show individual attempt durations and failure messages across the retry chain.

            **2. Check the failure rate driving retries.**
            Cross-check alert [#28 — Local Activity Execution Failed Rate Elevated](../alerts-index.md#alert-28--local-activity-execution-failed-rate-elevated) — a high failure rate with aggressive retry policy is the most common cause of this alert firing without alert #29 also firing. Individual attempts are short but the retry chain accumulates. Fix the underlying failure cause first.

            **3. Check whether WFT heartbeat timeouts have already occurred.**
            By the time this alert fires, the server may have already timed out heartbeat WFTs and rescheduled them — check worker logs for WFT timeout errors and check the history of affected executions for `WorkflowTaskTimedOut` events. If timeouts have occurred, local activities have already been re-executed from scratch — verify whether they are idempotent and whether any duplicate side effects need to be addressed.

            **4. Check whether LA slots are exhausted.**
            Cross-check alert [#14 — Worker Task Slots Exhausted](./14-worker-slots-exhausted.md) for `worker_type=LocalActivityWorker` — if multiple local activities are accumulating long retry chains simultaneously, all available slots can become occupied.

            **5. Fix the retry policy or design.**
            If the work genuinely requires many retries over a long period, convert to a regular activity with heartbeating — this is the correct primitive for work that may take a long time across retries. If it must remain a local activity, reduce the maximum retry attempts or increase backoff between retries so the total chain stays under 30 minutes, or set a `scheduleToCloseTimeout` less than 30 minutes so it fails before the server times out the WFT heartbeat.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/30-la-total-latency-timeout.md

      - uid: temporal-sdk-java-micrometer-031
        title: "Temporal SDK Java (Micrometer): Request Latency High on Critical User-Facing Operations"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, operation, le) (
                    rate(temporal_request_latency_seconds_bucket{operation=~"StartWorkflowExecution|SignalWithStartWorkflowExecution|ExecuteMultiOperation|SignalWorkflowExecution"}[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [2]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: request
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "7"
          summary: "Temporal SDK Java (Micrometer): p99 request latency > 2s on {{ $labels.operation }}"
          description: |
            **1. Check frontend service latency.**
            Check the [**Frontend Service Latency** panel (§4 — Service Latencies)](../../../dashboards/server/temporal-server-readme.md) filtered to the affected operations — if server-side latency is elevated, that is the root cause. The SDK-side metric includes serialization and network time, so server-side latency is the more precise signal.

            **2. Check persistence latency.**
            Check the [**Persistence Latencies** panel (§3 — Persistence Requests, Latencies and Errors)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `CreateWorkflowExecution` and `UpdateWorkflowExecution` — high persistence latency on these operations is the most common driver of elevated frontend latency for starts and signals. If the database is slow, the frontend service backs up and latency rises for all callers.

            **3. Check for RESOURCE_EXHAUSTED on critical operations.**
            Cross-check alert [#2a — RESOURCE_EXHAUSTED on Critical User-Facing Operations](./02a-resource-exhausted-critical-ops.md) — if the server is throttling these operations, the SDK retries add to the total observed latency on this metric. If both alerts are firing, RESOURCE_EXHAUSTED is the root cause of the latency.

            **4. Check frontend pod CPU.**
            If persistence latency looks normal and there is no throttling, check frontend pod CPU utilization via your infrastructure observability stack — a CPU-saturated frontend pod processes requests slower, directly increasing response latency.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/31-request-latency-critical-ops.md

      - uid: temporal-sdk-java-micrometer-032
        title: "Temporal SDK Java (Micrometer): WFT Execution Latency Critical (p99 > 10s)"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                histogram_quantile(0.99,
                  sum by (namespace, task_queue, le) (
                    rate(temporal_workflow_task_execution_latency_seconds_bucket[5m])
                  )
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [10]
                    type: gt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: NoData
        execErrState: Error
        for: 5m
        labels:
          severity: critical
          service: temporal-sdk
          component: wft
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "44"
          summary: "Temporal SDK Java (Micrometer): WFT execution p99 > 10s on {{ $labels.task_queue }} — WFT timeouts occurring"
          description: |
            **1. Check WFT replay latency.**
            Check `temporal_workflow_task_replay_latency` on the SDK dashboard — if replay latency is high, the time is being spent re-executing history rather than running new commands. High replay latency is typically caused by large history size, slow data converter execution during replay, or high sticky cache eviction rate forcing frequent cold replays. Cross-check alert [#35 — WFT Replay Latency High](../alerts-index.md#alert-35--wft-replay-latency-high) and alert [#34 — Sticky Cache Disabled](./34-sticky-cache-disabled.md).

            **2. Check sticky cache eviction rate.**
            Cross-check alert [#16 — Sticky Cache Forced Evictions High](../alerts-index.md#alert-16--sticky-cache-forced-evictions-high) — a high eviction rate forces cold replays on every WFT, directly driving up execution latency. This can be a compounding loop: timeouts cause evictions, evictions cause slow replays, slow replays cause more timeouts.

            **3. Check worker pod CPU.**
            If replay latency is normal but WFT execution latency is high, the time is being spent in new command execution — check worker pod CPU utilization. High CPU slows all code execution on the worker including WFT processing. Also check for blocking calls or heavy computation inside workflow functions — workflow code must be deterministic and fast.

            **4. Check for blocking workflow code.**
            Workflow code must not perform blocking I/O, heavy computation, or call non-Temporal APIs synchronously. Any blocking call inside a workflow function holds the WFT slot and inflates execution latency. For Python SDK workers, verify that no `async def` workflow code is blocking the event loop.

            **5. Check for RESOURCE_EXHAUSTED on respond operations.**
            Cross-check alert [#2b — RESOURCE_EXHAUSTED on Respond Operations](./02b-resource-exhausted-respond-ops.md) — if the server is throttling `RespondWorkflowTaskCompleted`, the SDK holds the slot until the respond call succeeds, inflating WFT execution latency even when the workflow code itself completed quickly.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/32-wft-execution-latency-critical.md

      - uid: temporal-sdk-java-micrometer-034
        title: "Temporal SDK Java (Micrometer): Sticky Cache Disabled"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: Prometheus  # DATASOURCE
            model:
              editorMode: code
              expr: |
                sum by (namespace, task_queue) (
                  temporal_sticky_cache_size
                )
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params: [1]
                    type: lt
                  operator:
                    type: and
                  query:
                    params: [A]
                  reducer:
                    params: []
                    type: last
                  type: query
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              refId: C
              type: classic_conditions
        noDataState: OK
        execErrState: Error
        for: 5m
        labels:
          severity: warning
          service: temporal-sdk
          component: cache
        annotations:
          __dashboardUid__: temporal-sdk-java-micrometer-v1
          __panelId__: "54"
          summary: "Temporal SDK Java (Micrometer): sticky cache disabled on {{ $labels.task_queue }} — every WFT is a cold replay"
          description: |
            **1. Check your worker configuration.**
            This is almost always a misconfiguration. Check the worker options in your application code:
            - **Go SDK** — `maxWorkflowCacheSize` set to `0` disables the cache entirely. The default is 10,000. Restore a non-zero value.
            - **Java SDK** — `WorkerFactoryOptions.Builder.setWorkflowCacheSize(int)` controls the cache size, default is 600. Setting it to `0` or negative resets to the default — the Java SDK does not allow disabling the cache this way. If this alert fires on a Java worker, check `WorkerFactoryOptions.Builder.setMaxWorkflowThreadCount(int)` — if the thread pool is set too low it can starve workflow execution and prevent the cache from being utilized effectively.

            **2. Verify the fix takes effect.**
            After correcting the worker configuration and redeploying, monitor `temporal_sticky_cache_size` on the SDK dashboard — it should climb from zero as the worker begins caching workflow executions. Also monitor `temporal_workflow_task_execution_latency` — it should decrease as cold replays are replaced by warm cache hits.

            **3. Check the impact on persistence and WFT latency.**
            While the cache is disabled, check the [**Persistence Latencies** panel (§3)](../../../dashboards/server/temporal-server-readme.md#3-persistence-requests-latencies-and-errors) filtered to `GetWorkflowExecution` — sustained high latency there confirms the server is under pressure from repeated history reads. Cross-check alert [#32 — WFT Execution Latency Critical](./32-wft-execution-latency-critical.md) and the `GetWorkflowExecutionHistory` long-poll rate on the SDK dashboard.
            Runbook: https://github.com/tsurdilo/temporal-metrics/blob/main/metrics/alerts/sdk/runbooks/34-sticky-cache-disabled.md
